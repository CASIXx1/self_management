version: '3'
services:
  db:
    image: mysql:latest
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
  web:
    build: .
    command: bundle exec foreman start
    volumes:
      - .:/myapp
    depends_on:
      - db
