<%= form_with(model: dairy, local: true) do |form| %>
  <% if dairy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dairy.errors.count, "error") %> prohibited this dairy from being saved:</h2>

      <ul>
      <% dairy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date, '日付' %>
    <%= form.text_field :date, class: 'datetimepicker', value: Date.today %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
